README

1. open gitLog.sh file and enter desired dates period to export logs, make sure you have entered right path as well in this file.
	then run on terminal -> sh gitLog.sh > out.txt
2. then run python code, out put file be generated. with the following columns
	then run TextToStructuredXLSX.py
	["Project", "Commit ID", "Day", "Date", "Timestamp", "Year", "Time Zone", "Message", "Lines Added", "Lines Removed"]